# Update — 2026-07-08 (please do this before your next slice)

We found and fixed some problems in the project docs. The old "All Slice Prompts" file had instructions that didn't match the plans — if your agent ever built something weird, that's why. It's retired now. There's also one new phase (2.5) and a cheaper, better model lineup for the video phase.

**You need ~20 minutes. Do these 5 steps in order.**

## Step 1 — Copy the new files into your repo

Your instructor will share these. Put them in your repo root, replacing old versions if they exist:

- `HOW-TO-DRIVE.md` ← your new workflow (3 prompts). Read this one now, it's short.
- `plan-phase-2-5.md` ← new phase between 2 and 3
- `plan-index.md`, `plan-phase-3.md`, `plan-phase-6.md`, `plan-phase-8.md` ← updated plans
- `agents.md`, `claude.md`, `replit.md` ← one new rule added
- `UPDATE-2026-07-08.md` ← this file
- Delete or replace your old `All Slice Prompts.md` — don't use it anymore.

## Step 2 — Run the drift check (one time)

Open a new agent session and paste the **"One-time catch-up"** prompt from the bottom of `HOW-TO-DRIVE.md`. It checks your repo for the known problems and fixes what it finds.

Skip this step only if you started the bootcamp after 2026-07-08.

## Step 3 — Add the new phase to your Linear board

Paste this to your agent:

```
Read plan-phase-2-5.md. On my Linear board, in the MVP Build project, create a milestone
"Phase 2.5 — Coverage & Asset Foundation" and one issue per slice (STU-C1 through STU-C7),
each with the Slice label, a description summarizing the slice's steps and acceptance
criteria from the plan file, and assigned to the new milestone. Set "Before/after composite"
as blocked by "Media library", and "Presenter library" as blocking the Phase 3
"Talking head / lip-sync video" issue.
```

## Step 4 — Clean your Linear board

If your board has duplicate issues (same title as a real slice, but not assigned to any phase milestone) — archive them. They confuse the agent.

## Step 5 — Use the new workflow from now on

`HOW-TO-DRIVE.md` has everything: one prompt to start a slice, one to say go, one to review (in a fresh session). That's the whole system.

**The one rule:** the `plan-phase-N.md` files + your Linear issue are the only spec. If anything else disagrees with them, the plan file wins — and tell the instructor.
